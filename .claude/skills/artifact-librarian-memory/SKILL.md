---
name: artifact-librarian-memory
description: The artifact-librarian agent's persistent behavioral memory (DEC-0331), preloaded into it at every spawn. NOT a general-purpose skill — never load this from context or by any agent other than artifact-librarian; it carries no instructions for you.
---

# artifact-librarian memory

Behavioral memory only (DEC-0331): task patterns, distillation
approaches that worked, common refusals and their causes, stakeholder
preferences. NEVER corpus facts — corpus truth lives in the artifacts;
an entry contradicting the corpus is a bug to fix here.

Curation rules (for the artifact-librarian, the sole writer of this
file): update via Bash only, as part of finishing a task; keep the
Entries section at or under 100 lines — consolidate or drop the least
useful entries when near the cap; one entry per line, most broadly
useful first; date entries (YYYY-MM-DD). This file is the ONLY thing
you may write outside the typed write API.

## Entries
- 2026-07-12 (SES-0082, close capture, eighth visit): `update-overview`'s post-write word-cap check counts differently from a plain `.split()` count of the source markdown file (my file measured exactly 250 by `str.split()` but the tool refused it as 251 and rolled back) — leave a few words of margin (e.g. target <=245) rather than cutting it to the exact 250 limit. Also final confirmation of the three-idea SES-0082 close-out pattern: append-turn (stakeholder verbatim + facilitator DEC-0261 capture-not-extend note) + create idea (captured status, proposed-by field, derives-from the session, relates-to the goals/decisions it discusses) + add-link (session relates-to new idea) in one apply batch, landed clean with the predicted next sequential IDEA ID; standalone update-overview afterward (append-turn/create/add-link never bump the overview's own --if-match token, reconfirmed yet again).
- 2026-07-12 (SES-0082, close-out DEC-0267 batch): a 10-op single `write apply` batch (append-turn + edit-section x4 (2x BG-0001 Intent/System-Context, 1x EP-0006, 1x EP-0008, all `--amend` since all three artifacts stayed `approved` throughout — DEC-0267 re-affirm-not-re-gate needs zero status transitions) + add-cite x4 + update-overview) landed 100% clean when every edit-section's replacement content was built by string-replacing only the target passage inside the section's freshly-read full body (never hand-retyped), and every newly-added DEC-0420 citation was matched with `add-cite` in the same batch — confirmed BG-0001 (business-goal) IS in DEC-0247's cites-sync set (not exempt like decisions). CONTEXT.md glossary still has zero typed write path; direct Python-script file edit (string-replace on the exact old block) via Bash remains the only route, verified after via `check` (glossary isn't scanned by the checker directly, but downstream artifact cite/body consistency is). `graph impact <ROOT-BG>` returns the ENTIRE derivation tree (hundreds of descendants) with no way to scope to "artifacts affected by this specific edit" — not useful alone for a narrowing-edit staleness judgment; the real signal is grepping body prose corpus-wide for the specific echoed phrasing being narrowed (e.g. `grep -rl "Groundwork is a standalone application"`) to find which artifacts actually need edits, then trusting the caller's own bulk-citer-review disposition for the rest rather than treating the full impact-walk list as a stale-marking worklist.
- 2026-07-12 (SES-0082, third visit): append-turn's auto-assigned numbering correctly continued from T25 to T26-T28 in a single --from-file multi-turn payload (### T99 placeholders x3) against a session already using ### T-heading style throughout — no collision, reconfirms the numbering quirk is legacy-artifact-specific only. update-overview succeeded on the SAME token (v:aa6010bb74f0) returned both before and after the append-turn call — append-turn does not appear to bump the overview's own --if-match token (only content/section edits seem to); always re-read overview immediately before update-overview regardless, since relying on token stability across ops is unverified/risky in general.
- 2026-07-12: run the PROJECT-LOCAL CLI — `<project>/.claude/skills/artifact-interact/scripts/gw.py` — without find-searching for it each spawn; the `~/.claude` copy can lag the canonical project copy. For op semantics, flags, refusal conditions, and batch-JSON keys, read `references/operations.md` (kept code-accurate as of 2026-07-12) or `write <op> --help` (per-op descriptions now document batch keys) — do NOT read gw_write.py source to figure out usage; if behavior contradicts operations.md, that mismatch is itself a reportable defect. If the project has an uncommitted fix to gw_write.py, `git status`/`git diff` there first and use the project-local copy regardless.
- 2026-07-12: the rule-22-style placeholder-token ratification guard (DEC-0378, on set-status to approved/accepted/closed) only exempts backtick/fenced code spans for an unallocated `PREFIX-XXXX` mention — a legitimate historical narration wrapped in plain double quotes (e.g. the transcript text `the body H1 read "SES-XXXX"`) still trips it, REFUSED with a reported line number that can be a few lines off from the actual grep hit (don't be thrown by that — `grep -n XXXX` on the whole file finds the true offending line regardless of what the refusal message states). No sanctioned append-turn/edit-section path fixes an already-appended turn's text (transcript is append-only) — this is a genuine STOP-and-report case, not a workaround-in-API situation; the caller/operator applying a direct quote-style-only fix (no semantic change) is the correct resolution, same category as the other operator-sanctioned direct edits this session already used. Also reconfirmed: rule 18 (DEC-0250, session relates-to must be body-referenced) applies to sessions of ANY status, not just ratified ones — an open session's freshly added relates-to link needs a body mention immediately, same as a closed one (Post-Close Enrichment is only the closed-session variant of satisfying the same rule).
- 2026-07-12: `write update-overview`'s batch-apply key is `text`, NOT `overview` (unlike `create`'s `--overview` CLI flag) — passing `"overview": "..."` in a JSON apply op silently defaults to `text=None` (build_namespace's default dict has no `overview` key) and fails with "provide overview text via stdin, --from-file, or --text"; verified via `write update-overview --help`. Also: DEC-0250 rule 18 ("session relates-to X but body never references it") fires just as hard on a session's OWN newly-created decisions as on backfilled historical ones — when a session task creates decisions AND adds them to its own relates-to in the same pass, the natural fix isn't a Post-Close Enrichment turn (the session is still open) but filling in the session's own "Decisions Produced" section with the real IDs — it's required content anyway (SPEC-session) and satisfies rule 18 for free; mentioning IDs only as a compressed range ("DEC-0376 through DEC-0385") in the overview does NOT satisfy it either, since rule 18/mentions scans body prose only, never the overview.
- 2026-07-12: SES-0072 rule-23 relates-to backfill (session→DEC cross-reference enrichment, DEC-0248) — a single `write apply` batch of 373 add-link ops across 65 sessions ran clean in one shot (generated the ops by regex-parsing the full `check` output's rule-23 lines directly, no manual enumeration needed); post-batch, a full `check` showed zero rule-23 errors and zero rule-18 (DEC-0250) fallout, confirming relates-to-only additions don't by themselves trigger body-mention requirements the way cites do.
- 2026-07-09 (CORRECTED 2026-07-10): `write apply`'s forward-reference gap noted here is FIXED as of 2026-07-10 — `resolve_all` now defers into `pending_resolve` under `defer_recheck` (same mechanism as `recheck`) and is flushed after all ops run, so body-prose/section-content forward references within one batch now resolve correctly. What is still NOT deferred: `create`'s own `--link`/frontmatter link-target-existence check runs immediately per-op, so an ID used as a `--link relates-to=X` (frontmatter link, not body prose) must already exist earlier in the same batch — order creates so link targets precede their referrers; only body/section *content* references get the batch-wide deferral.
- 2026-07-09: after a batch apply partially fails (e.g. forward-reference ordering, see above), check `git status` before retrying — untracked newly-created files can be safely removed and re-attempted, but do NOT re-run already-succeeded `create` ops (they'd allocate new duplicate IDs); instead build a remaining-ops batch of only the unexecuted ops, reordered as needed.
- 2026-07-09: `search audit` (and other `search`/`graph` families, run via `uv`) print progress bars and warnings to stderr — when redirecting a family's output to a saved JSON packet, redirect stdout only (`> file.json`, not `> file.json 2>&1`) or the file becomes invalid JSON.
- 2026-07-10: don't trust a caller's claim that source files were "already corrected" — verify by grepping the actual body files for the flagged token (e.g. a dangling ID like EP-0009) before applying; in one case the caller said a forward reference was removed but it was still present in two of three body files, causing the same post-batch validation failure again. Post-write check re-confirmed it independently either way, but a pre-flight grep would have surfaced the discrepancy before spending a write op.
- 2026-07-10 (VERIFIED 2026-07-12: no longer applicable): this memory file's project-local copy is canonical; a global `~/.claude/skills/artifact-librarian-memory/` copy no longer exists at all (checked 2026-07-12) — project-local is the only copy, no drift-check needed going forward.
- 2026-07-10: for a stakeholder-authorized one-off manual out-of-band edit (outside the write API), verify with `read outline` (heading appears once, etc.) then run `check` — do not re-derive correctness from the diff yourself; the checker's verdict line is the report.
- 2026-07-12 (REVERSED, DEC-0399): code-span-wrapped IDs in body prose no longer reliably trip `write apply`'s post-write recheck — DEC-0399 gave the recheck's body ID scan code-span parity with the full checker (same code-stripping walker). Treat an apply-time "unresolved ID" refusal as a REAL problem until proven otherwise; do not shrug it off as noise anymore. The raw frontmatter/overview scan is intentionally NOT code-span-aware (see the separate overview entry below) — that gap is by design, not a bug.
- 2026-07-12 (SETTLED, DEC-0398): overviews may NEVER contain unresolvable artifact-ID tokens — backticks grant no exemption on the overview surface (the body-scoped code-span carve-out, SPEC-artifact-common rule 7, still stands for body Markdown only). If a task's literal requested overview text embeds an unresolvable ID, refuse-and-report immediately, citing DEC-0398 — do not attempt the write first; rewording the ID away is the caller's choice, not mine.
- 2026-07-10 (PARTIALLY SUPERSEDED, DEC-0407): `write create --type idea` needs `--field 'proposed-by: <name>'` (separate from `--owner`, per SPEC-idea) plus `--link relates-to=...`. Body-via-`--from-file` used to be verbatim with no scaffolding; `create` now structurally gates bodies (see the 2026-07-12 DEC-0407 entry below) — the create-then-`edit-section`-each-required-section tactic is still a valid, safe workflow (avoids needing the allocated ID before writing the H1), just no longer the only way to pass the gate.
- 2026-07-10: SPEC-idea's `derives-from: [SES-....]` is explicitly optional ("the spawning session, when there is one") — if `status --root . report` shows all sessions closed (no open SES-*) at capture time, it is correct to omit derives-from rather than guess/force a stale or nonexistent session ID; note the gap to the caller instead of fabricating a link.
- 2026-07-10: cross-referencing a still-`captured` Idea (e.g. adding relates-to + a short Disposition note pointing to a follow-up idea) is safe and within guardrails — captured ideas aren't gate-locked like accepted decisions; edit-section works directly, no `--amend`/supersede needed.
- 2026-07-10: `write create --link relates-to=A,B,C,D` on a session (or any type under DEC-0250) requires EVERY linked ID to also appear as a bare-ID mention in body prose, not just in frontmatter links — `create`'s own re-check does not catch this (it passed), only the full `check` gate does (FAIL: "relates-to X but body never references it"). When the task's specified T1 verbatim proposal text doesn't happen to mention all relates-to IDs, add a follow-up turn (e.g. a facilitator restatement, consistent with the intake protocol's T1=proposal/T2=restatement shape) that naturally references the remaining ones, then rerun `check` to confirm.
- 2026-07-10/11: `relates-to` is NEVER auto-reciprocal (unlike `impacts`/`impacted-by`, which DO auto-reciprocate) — this holds for `create`'s frontmatter links, `add-link`, and even on accepted decisions where relates-to is the sanctioned DEC-0248 enrichment link. For genuine two-way discoverability, issue two explicit `add-link ID1 relates-to ID2` / `add-link ID2 relates-to ID1` calls and verify both sides landed — the post-write recheck never requires or checks relates-to reciprocity.
- 2026-07-10: confirmed working end-to-end: when a finding narrows/corrects an accepted decision that still carries valid surviving content, use `create` (a new accepted decision, own body, own decided-by/decided-on/source-span fields set directly at create time via `--status accepted --field ...`) plus `add-link OLD relates-to NEW` on the old decision(s), rather than `supersede` — precedent DEC-0346 narrowing DEC-0338, repeated for DEC-0347/0348/0349 narrowing DEC-0292/0329. Follow with `consistency sweep` and `consistency terms` on the new DEC IDs to catch stale citer content and missing-link candidates the narrowing may have surfaced.
- 2026-07-10: `consistency terms` flags co-occurrence on ANY rare-ish shared token, including generic lifecycle/status vocabulary (e.g. two unrelated decisions both mentioning "taken-up") — these are noise, not missing links; only act on hits where the shared term is a substantive proper-noun-like token (a model string, an artifact-specific mechanism name). Report noise hits to the caller as reviewed-and-judged-not-actionable rather than silently adding links for every hit.
- 2026-07-10: the decision-recall `search audit ID` op returns a judge packet (considered IDs + ranked candidates + judge_instructions) that the librarian must NOT self-judge — pass it through verbatim to the caller/judge subagent; do not pre-filter or rank-reorder it yourself.
- 2026-07-10: when a task says "resolve epic chartered by DEC-X" and asks to gate downstream writes on its approval — check the epic actually EXISTS first, not just its status; a DEC can charter an epic that was never derived (see IDEA-0015/SES-0061: DEC-0339 charters an epic that was never created). `graph impact <DEC-ID>` with zero derivation descendants is a fast independent confirmation of "no such epic" alongside grepping docs/epics/. When the gate fails, treat the whole downstream write chain (decisions AND spikes, not just the spikes literally named) as blocked if the decisions' content presupposes the missing artifact — only append the session transcript and update the overview to state the block accurately; don't half-apply prerequisite decisions that reference a nonexistent epic.
- 2026-07-10: creating an epic whose title/body literally names a not-yet-created ID (e.g. a DEC titled "EP-0009 derives with...") fails post-write check as an unresolved reference even though the batch as a whole is coherent — the ID token in a title is checked like any body reference. Order: create the epic FIRST with placeholder/no forward-referencing text for content that would cite not-yet-created sibling decisions (use bracketed placeholders like `[SPIKE-SCOPE-DEC]`), then create the decisions (which can now safely reference the real epic ID), then `edit-section` the epic to substitute real DEC IDs for the placeholders and `add-cite` them in. DEAD as of 2026-07-12 (DEC-0411..0416): a `create` op that fails this way now ROLLS BACK — nothing lands on disk, so do NOT skip recreating it; recompose and re-run the op/batch once the dependency exists. Also: `write --json` goes directly after `gw.py`, before `--root`/family (e.g. `gw.py --json --root . write create ...`), not after the family.
- 2026-07-10: when an epic's body prose cites decisions beyond its initial `--cite` list (common — good prose naturally mentions related DECs), the full `check` flags every such gap as `body references DEC-X but cites: omits it` (DEC-0247) — after drafting an epic's full body, grep it for all `DEC-\d+`/`BG-\d+` tokens and reconcile against frontmatter `cites:` with a batch of `add-cite` calls before considering the task done; also check whether the epic's parent goal (BG-*) needs its own `Derived Work` section updated to name the new epic (DEC-0246) — approved goals need `--amend` on `edit-section` for this.
- 2026-07-10: CONTEXT.md (the glossary) has no typed write op — no `id:` frontmatter, so `edit-section`/`update-overview` don't apply to it, and no dedicated glossary-write op exists in gw_write.py. It lives outside `docs/`, so DEC-0312's docs/-artifact prohibition doesn't literally cover it; direct textual edit (matching the file's existing entry format) is the only path available. Verify with `check`/`consistency terms` afterward same as any other change.
- 2026-07-10: `edit-section` REPLACES the entire section body (heading kept) — when a section already carries a DEC-citation sentence unrelated to the new addition (e.g. a "per DEC-0345 ... DEC-0336" validation-note sentence embedded mid-Method), read the full current section text first and preserve every existing sentence/cite in the new content, appending rather than reconstructing from a partial mental summary. Dropping an existing cite-bearing sentence silently creates a "dead cite" FAIL on the full checker even though the per-op re-check passes clean.
- 2026-07-10: when a new body sentence you're writing itself references a DEC not yet in the artifact's frontmatter `cites:` (e.g. adding a Data-Source Assumptions line that mentions DEC-0340), add-cite that DEC in the same batch — the per-op re-check does not catch "body references but cites: omits" (DEC-0247), only the full `check` does.
- 2026-07-10: SPEC-spike defines NO post-execution status distinct from the common gate lifecycle (draft/in-refinement/gated/approved/stale/superseded/archived) — "record findings at spike completion" does not imply a status transition; an approved spike whose Findings/Resulting Decisions sections get filled in place stays `approved` (edit-section on an approved spike needs `--amend`), and the caller should be told explicitly that no evaluated/complete status exists rather than inventing one.
- 2026-07-10: a 14-op single `write apply` batch (edit-section w/ --amend, create DEC, edit+add-cite a sibling spike, 3x create idea, edit-section + two set-status on another spike, 3x append-turn, update-overview) ran clean end-to-end when ordered creates-before-references — confirms the batch size/complexity ceiling is generous as long as forward-reference ordering (memory entry above) is respected; still always finish with a standalone full `check` since apply's own post-batch recheck does not catch DEC-0247 cites-omits-body-reference gaps (caught 2 on this run, fixed with 2 follow-up `add-cite` calls).
- 2026-07-10: a coordinator can resend an instruction that already landed (delivery/completion race) — before re-running a write batch (especially append-turn/set-status closed), re-read the target artifact's current state; if the requested turns/status/overview are already present, do not re-execute (append-turn on a closed session is refused anyway, but set-status/overview could silently duplicate or no-op in confusing ways) — report that it's already done instead.
- 2026-07-10: `--overview-ok` is an `edit-section` flag only (silences the DEC-0288 same-edit-overview reminder) — `write update-overview` itself takes no such flag and errors "unrecognized arguments" if passed; just omit it when calling `update-overview` directly.
- 2026-07-10: Uses:-line element backfill on an approved CMP (SES-0066 batch pattern) — build the full new Design Elements section content locally (script-insert `Uses: ...` right after each element's `Implements:` block, preserving continuation-line style: flush-left no-indent wrap for paragraph fields like Implements:/Uses:, mirroring existing file conventions) then one `edit-section --amend --overview-ok` per touched section (Design Elements, Dependencies), plus `add-link depends-on` for new component edges, plus the session turn — all in one `write apply` batch. Verify via `tools/check_links.py --uses-advisory`: diff the full before/after finding lists (not just counts) to confirm exactly the target CMP's own findings cleared and nothing else moved.
- 2026-07-10: SES-0066 batch 4/4 (final) confirmed the batch-3 payload-guard pattern scales: 8-op `write apply` (5x edit-section Design-Elements, 1x edit-section Dependencies-section addition, 1x add-link depends-on, 1x append-turn) applied clean in one shot when every content payload was pre-checked with a grep for the target `## heading` string before apply. When a caller reports an external defect alert mid-task (e.g. "duplicated sections" from another session/agent), verify independently against the working tree (heading-count grep + `git diff` magnitude/shape check) before reacting — do not assume the alert is accurate or that no action is needed just because it sounds resolved; report the verification method and result, not just the conclusion.
- 2026-07-10 (WORKAROUND DEAD as of 2026-07-12): no write op creates a brand-new top-level body section, and the old trick (append-turn content opening with the new `##` heading) is now REFUSED by the heading guard. There is currently NO sanctioned path to add a missing required section — STOP and refuse-and-report, citing IDEA-0042 (sanctioned-repair override for API-unreachable surfaces).
- 2026-07-10: when a decision's body prose narrates later-created sibling decisions by ID (e.g. an SP-0014 note saying "precondition met (DEC-0365)"), each mentioned ID needs a matching `add-cite` — rule 16/DEC-0247 fires immediately on `check`, easy to miss when drafting cross-referencing prose inside a batch of otherwise-unrelated edits.
- 2026-07-12 (UPDATED, DEC-0400/DEC-0401): `create`'s batch link field is `"links": {"derives-from": [...], "relates-to": [...]}` (dict of lists, matching CLI namespace attr `links`) — NOT a CLI-style `"link": "rel=ID,ID"` string. As of DEC-0400 this is no longer silently dropped: `write apply` now pre-validates the ENTIRE batch before applying anything, and refuses wholly on unknown op-spec keys, rel keys outside the closed vocabulary, or unknown op names. Other batch keys are positional-arg names (`add-link`: `id`/`rel`/`target`; `edit-section`: `id`/`heading`/`content`) — verify unfamiliar keys via `write <op> --help` (per-op descriptions now document batch keys).
- 2026-07-11: when a body-prose correction cites a brand-new sibling DEC by ID (e.g. "now a standing dependency (per DEC-0366)" written into the same CMP the DEC is about), that's a forward reference within the *same* edit — do it as a follow-up `edit-section` + `add-cite` pair AFTER the `create` op that allocates the DEC ID, not inside the same apply batch unless the create precedes the edit-section referencing it; either way still run a final `add-cite` for the new DEC since `check`'s DEC-0247 body-references-but-cites-omits rule fires on it same as any other cross-reference.
- 2026-07-11: for a concurrent-session task where SES ID must not be hand-picked, just call `write create` (no `--id`) and trust the API's sequential allocation, then run `write append-turn` as separate follow-up calls addressed to the returned ID — do not build a single `write apply` batch that both creates the session and appends its turns, since the ID is unknown until create returns.
- 2026-07-11: preliminary/spike-in-progress decisions use status `proposed` (the create default for type=decision) — no `--status` override needed; just don't call `set-status ... accepted`. A caller instruction like "PRELIMINARY, do not accept" maps directly to leaving status at its default.
- 2026-07-11: redo of the IDEA-0041 heading-duplication defect (SP-0016, after caller reverted the corrupted file to HEAD) succeeded clean in one `write apply` batch by grep-verifying zero `^#`-heading lines in prepared Findings/Resulting-Decisions content files before the call, then re-grepping heading counts (`grep -c "^## Findings"` etc. = 1) on the result before reporting done — the discipline from the prior incident generalizes; always do the pre-write grep AND the post-write count-check, not just one.
- 2026-07-11: `set-status` to `approved` from `draft` is refused — the gate lifecycle (SPEC-artifact-common) is linear (draft -> in-refinement -> gated -> approved), no skipping; when a task says "walk status to approved" on a draft artifact, batch all three transitions (in-refinement, gated, approved with `--approved-by`) in order, not a single jump.
- 2026-07-11: before creating decisions/artifacts a task instructs you to produce, check whether they already exist on disk (grep `docs/decisions/DEC-XXXX*` or similar) — a prior session may have already created them (e.g. via SES-ID cited in the task) even though the specific file you were asked to redo was reverted; recreating would duplicate. Confirmed here: DEC-0374/DEC-0375 already existed, accepted, well-formed from SES-0071, only SP-0016 itself needed the redo.
- 2026-07-11 (repair path updated 2026-07-12): for duplicate back-to-back `## Heading` corruption in legacy artifacts, detect via `grep -c '^## Heading'` before editing; repair is in-API — `delete-section <id> <heading> --occurrence 1` for the empty phantom (checker rule 21 also flags every instance corpus-wide).
- 2026-07-11: confirmed DEC-0247's "body references DEC-X but cites: omits it" gap applies to spikes exactly like epics/decisions — a spike Findings/Resulting-Decisions rewrite that narrates many DEC IDs (including brand-new proposed ones from the same session) needs a full `grep -o 'DEC-[0-9]\{4\}'` sweep against the frontmatter `cites:` list and a batch of `add-cite` calls before the final `check`; per-op edit-section re-checks never catch this, only the full checker does. Also reconfirmed: `set-status` draft->approved must walk in-refinement->gated->approved (no skip) even on a spike, and the approved-on date the API stamps is today's date regardless of what the task text literally spells out — treat "use current date if supplied" instructions as satisfied by the API's own stamp, don't force-override.
- 2026-07-12: SES-0072 shipped the IDEA-0041/IDEA-0028 fixes — several older entries above are now stale in their "no recovery exists" claims. Current behavior: (1) `edit-section`/`append-turn` REFUSE payloads containing a heading line at the target section's level or higher — the pre-write heading grep is now enforced by the tool itself (keep doing it to fail fast, but the tool is the backstop). (2) Phantom-heading corruption IS now repairable in-API: `edit-section --occurrence N` targets the Nth matching heading; `delete-section <id> <heading> [--occurrence N]` removes a duplicate occurrence (it refuses to delete the last occurrence of a type-required section, closed sessions, accepted decisions; approved/stale need `--amend`). Never freehand-edit for this anymore. (3) Ratifying `set-status` (approved/accepted/closed) refuses bodies with duplicate sibling headings or placeholder lines (standalone TBD/TODO/FIXME, unallocated `…-XXXX` IDs); quote placeholders in backticks when mentioning them legitimately. (4) Session close requires participant/participant-role/facilitator/transcript-fidelity frontmatter AND either a produced decision or `--no-decisions-ok "<reason>"` — when a close is refused for zero decisions, surface the warning to the caller for an explicit assessment; do not invent the flag yourself. (5) `set-status accepted` on a decision requires `--session SES-nnnn` and stamps `accepted-in`. (6) `create --from-file` now stamps the allocated ID into a leading `# PREFIX-XXXX: …` H1 — author body H1s exactly that way instead of pre-deriving the next ID. (7) New checker rules 21-24: duplicate sibling headings (all artifacts), placeholders in ratified artifacts, session relates-to must include every DEC deriving from it (spikes: via cites), body H1 must carry the artifact's own ID.
- 2026-07-12: predicting the next sequential ID (`ls docs/<dir> | grep -oE 'PREFIX-[0-9]+' | sort -V | tail -1`, then +1) immediately before a single-writer `write apply` batch is safe for forward-reference body/transcript text (e.g. a session's T-final turn naming the decision/idea it produces before they're created later in the same batch) — body content isn't resolve_all-checked at `create` time at all, only at `edit-section`/`update-overview`/batch-deferred recheck, so a correctly-predicted ID in a create body never trips validation; confirmed by a 8-op batch (create session/decision/idea + 5 reciprocal add-link relates-to calls) landing with zero mismatches and a clean full `check`. Still respect the create-op's own immediate (non-deferred) frontmatter `--link`/`links` target-existence check — order creates so link targets (including a same-batch session as a decision's derives-from) precede the op that links to them.
- 2026-07-12 (merged from a drifted copy): to APPEND content to an existing section via edit-section (it replaces, never appends), read the section first (`read section`), strip the tool's header-echo line and the `## Heading` line from the output, then write back stripped-original + new paragraphs as one body-only payload — never send only the new paragraphs; that deletes the existing content.
- 2026-07-12: an intake-opened take-up session (T1 stakeholder verbatim, T2 facilitator restatement, T3 stakeholder confirm — DEC-0273/0274 shape) that is left OPEN (not closed) needs no decisions/no placeholder-guard handling at all — the ratifying-transition placeholder/decision gates only fire on set-status to approved/accepted/closed, so `create --status open` with populated Purpose/Transcript sections and stub 'Decisions Produced'/'Conflicts Raised' text (no TBD placeholders needed structurally, but avoid literal 'TBD' anyway) passes clean. Frontmatter for such a session: `kind: full`, `intake: {origin: user, proposed-by: <person>}` (source-ref only when origin is cp/idea), `transcript-fidelity: reconstructed` when reconstructing after the fact. For idea take-up: `set-status taken-up` is NOT a ratifying transition either (no placeholder guard), and two real precedents (IDEA-0028, IDEA-0041 at SES-0072) did NOT add the take-up session to the idea's own `relates-to` — only the idea's `derives-from` holds its *spawning* session; if a caller explicitly asks for the take-up SES added to the idea's relates-to anyway (as a caller instruction can), that's a legal explicit add-link, just note it diverges from the two observed precedents.
- 2026-07-12: DEC-0246 (epic body must reference every derived child) fires immediately on `check` when a new spike/CMP/story is created with `derives-from: [EP-X]` — even when EP-X's status is `stale`/`approved` (needs `--amend` to edit) and the task's caller has explicitly instructed "do not edit this artifact's body" for an unrelated reason (e.g. a pending stakeholder-ratified staleness cascade). Do not resolve this yourself by editing the epic anyway — it's a genuine conflict between two task requirements; leave the `check` FAIL standing and report it verbatim as an open item alongside the other cascade items for the caller/stakeholder to sequence (which consistency edit lands first). Also reconfirmed: `set-status <id> stale` (approved->stale) needs no `--reason`/no body edit — there is no reason field in the API; the status transition itself is the durable record (DEC-0267: "the stale marks themselves are the durable record"), so satisfy a "mark stale with reason X" instruction by performing the transition and stating the reason in your own report, not by writing it into the artifact.
- 2026-07-12 (UPDATED, DEC-0403): `remove-cite` now exists — "remove a DEC citation (dead-cite repair)", refuses when the target is absent from cites, when code-stripped body prose still references the DEC, or on immutable statuses (accepted/closed; approved/stale need `--amend`). `remove-link` still does NOT exist. The "(NEW, superseding OLD)" single-mention convention (mirrors SPEC-artifact-common's own example) remains a valid choice for keeping an old cite legitimately referenced, but is no longer the only path — `remove-cite` after reworking body prose to drop the mention is now available too.
- 2026-07-12 (UPDATED, DEC-0401): `write apply` bash calls can still appear to time out (exit 143 after the 120s default) — but as of DEC-0401 the apply loop emits a flushed per-op result line as each op lands plus a terminal applied-N-of-M summary, and on failure prints a manifest of applied/failed/not-attempted ops, so this manifest (not an educated guess) is now the source of truth for what actually landed; still diff-verify targets after an apply as a second check. Prefer `--from-file <absolute-path>` over inline heredoc substitution for any nontrivial `--text`/payload argument (a heredoc-in-command-substitution background Bash call hung indefinitely once), and always pass `--root` as an absolute path. NOTE: the `.gitignore` `SP*.md`-ignores-all-spikes claim from earlier is FALSE as of commit b2625c9 — the pattern is now anchored `/SP*.md` and `git check-ignore docs/spikes/*.md` matches nothing; spike files are tracked normally, verify via git status/diff same as any other artifact.
- 2026-07-12: creating 10 accepted decisions in one derives-from=SES-0077 batch reconfirmed: (1) mutual pair-link relates-to between two same-batch new DECs can't be set at create (forward reference on create's own immediate link-check) — create both without the mutual link, then two `add-link relates-to` calls after, same pattern as any other new-DEC cross-link; (2) rule 23 (session produced DEC but relates-to omits it) fires per-DEC even when the session's own "Decisions Produced" body section already names every ID — body mention alone never satisfies rule 23, only an actual frontmatter relates-to link does (add-link separately, one call per DEC, safe to batch); (3) `create --status accepted` with `derives-from` the current open session needs no `--session` flag (accepted-in stays unstamped, correct — only later ratification stamps it); (4) `--field 'decided-by: X'` stamps the literal string given verbatim (no auto-suffix), so pass the full form the caller wants stored.
- 2026-07-12: when authoring a caller-supplied body for `create` (now structurally gated — DEC-0407 requires the type's full section skeleton), write each required section SUBSTANTIVELY, not as skeleton filler: map the task's actual material into the section it belongs in — e.g. for a decision, Context = the question that had to be answered, Decision = one unambiguous statement, Rationale = the why, Alternatives Considered = what was on the table, Implications = the consequences. The skeleton guard only checks headings exist; it does not check content quality — that responsibility is mine. When the task genuinely gives no material for a section, say so plainly in that section (don't pad with generic filler) and flag the gap in the report. Origin: SES-0077's T16/T17 finding that DEC-0388..DEC-0406 shipped flat (batch-created with inline blob bodies, zero/partial section structure) precisely because bodies were authored as undifferentiated prose rather than per-section content, requiring a 19-decision operator-sanctioned direct-edit repair.
- 2026-07-12: `create --from-file` body H1 must be predicted right up to the actual create call — a same-project concurrent write task can allocate an intervening ID between my `ls docs/sessions | tail -1`-style prediction and my `create`, so a body authored with `# SES-0078: ...` landed as the real `SES-0079` (frontmatter id correct, only the literal H1 text stale). `create` does NOT auto-stamp a real-but-wrong ID (only the literal `XXXX` placeholder pattern gets stamped, per DEC-0384) — REFUSED post-write with 'body H1 names SES-0078, not SES-0079 (rule 24)'. DEAD as of 2026-07-12 (DEC-0411..0416): this refusal now ROLLS BACK — nothing is left on disk; a repeat of this race just means re-running the create with the correct H1, no disk cleanup needed. This is IDEA-0042's captured 'body H1s' API-unreachable surface: no write op can retarget an H1 after the fact — refuse-and-report to the caller rather than freehand-editing myself, even though it's a one-token mechanical fix; frontmatter/relates-to/overview on the file were otherwise all correct and the artifact reads fine via `read overview` despite the checker FAIL.
- 2026-07-12: create's batch payload key for body content is "from-file" (hyphenated, matching the CLI flag name) per operations.md's table ("content or from-file") -- I initially typo'd it as "from_file" (Python-attr style, wrong) while drafting, caught it against operations.md before running, and did not test what an unhyphenated key actually does (unverified whether it errors or silently no-ops); use the hyphenated form and verify the created file's actual body against your intended content regardless. Also reconfirmed in the same batch: predicting the next sequential ID immediately before a single-writer apply (no concurrent write task observed) is safe for body-text forward references (a T9 turn plus a session's own Decisions-Produced section citing the not-yet-created IDEA-ID) as long as the create op precedes any add-link targeting it in the same batch; a session's rule-18 relates-to-must-be-body-referenced requirement is satisfiable by editing the type-required Decisions-Produced-style section to add a plain bare-ID mention, not just an overview mention (overview mentions never count) -- reusing an existing required section this way avoids needing any new/heading-adding section.
- 2026-07-12: design-gate close-out pattern (open session, six sibling decisions, XXXX-placeholder H1s to dodge ID-prediction races): using `# DEC-XXXX: ...` (not a predicted number) sidesteps the concurrent-allocation H1 mismatch entirely — safe even when other write tasks may be running, since DEC-0384's placeholder stamping handles the real ID regardless of allocation order. Full-batch order that worked clean in one shot (10 ops: 4x append-turn + 6x create accepted-decision): append-turn calls first (no ID dependency), then all six creates with `links: {"derives-from": ["SES-open-id"], "relates-to": [...]}` — DEC-0391 relates-to plus zero-to-two extra sibling relates-to per decision, each verified to already exist beforehand via `read overview`. Second batch then did: 6x add-link (session relates-to each new DEC, rule 23), one edit-section on the session's existing "Decisions Produced" section naming every ID in body prose (satisfies rule 18 — overview mentions/ranges never count, confirmed again), one update-overview, plus mutual back-links (`add-link OLDDEC relates-to NEWDEC`) for any extra-link decisions cited (relates-to is never auto-reciprocal). Both batches applied 100% clean, full `check` OK. `consistency sweep` on new DECs relating to a widely-cited parent (e.g. DEC-0391) reliably flags the parent's governing epic (EP-0009 here) as REVIEW even when the new decisions are just implementation refinements already covered by the epic's existing DEC-0391 citation — legitimate to disposition as "reviewed, no epic edit needed" rather than auto-editing the epic, since epic-body edits are a separate, larger judgment call outside a design-gate task's scope. `consistency terms` hits on generic write-op vocabulary (add-link, edit-section, update-overview) shared with DEC-0313 (the op-set decision) are reliably noise, not missing links.
- 2026-07-12 (DEC-0391 shipped: SES-0079, DEC-0411..0416): the concurrency contract is now real, not just designed. (1) Writes serialize at the APPLY MOMENT on an exclusive `flock` (`<root>/.groundwork-lock`) — corpus scan through graph sync is one transactional unit; reads/search/graph take the same lock shared, so parallel write-task librarians are now safe (they queue, up to 60s default, `GW_LOCK_TIMEOUT` overrides; a timeout refuses cleanly naming the holder PID — retry, don't force). The DEC-0394 "one write-librarian at a time" interim restriction is obsolete. (2) Applies are transactional: ANY failure (refusal, recheck, crash) rolls the corpus back via the journal (`<root>/.groundwork-journal/`) — "file written, fix required" is dead everywhere in this file; a refused write leaves NOTHING on disk. After a refusal, recompose and re-run the op/batch; never treat a partially-landed file as real, and never skip recreating something because "the blocking ID exists now" — it doesn't, unless the create actually succeeded. (3) Turn numbers are tool-assigned: `append-turn` renumbers from the live transcript max and reports `assigned Tn`/`assigned T7-T9` in the OK line — stop predicting turn numbers in composed content; `--expect-first-turn N` is an optional precondition-check only. (4) `edit-section` and `update-overview` now REQUIRE `--if-match <token>` from a current read (read section/overview print it; read section supports `--occurrence`; outline shows per-`##`-section tokens) — exempt only for artifacts created earlier in the same invocation/batch. Read-before-edit is now structurally mandatory, not just good practice. (5) ID-prediction races on `create` body H1s can still make a composed H1 stale (concurrent allocation), but the XXXX-placeholder pattern (already preferred practice, see the design-gate close-out entry above) remains correct, AND a wrong-ID H1 create now rolls back cleanly instead of landing broken — no more IDEA-0042-style stuck-file refuse-and-report for this specific case, just retry with the real ID.
- 2026-07-12 (SES-0079 close-out): `append-turn`'s auto-numbering regex is `^(###\s+T|\*\*T)(\d+)\b` — a composed heading using a non-digit placeholder like `### T?` does NOT match, so the tool silently leaves it un-renumbered and it lands on disk verbatim as literal "T?" (no error, no OK-line warning distinguishing it from a real assigned number). Compose turn headings with any real digit placeholder (e.g. `### T99`) rather than a `?` or other non-numeric marker; if this mistake lands anyway, the sanctioned repair is `edit-section <SES> "Transcript" --if-match <token>` replacing only the malformed heading digits within the full current section body (read section first, strip the header/heading-echo lines, keep every other character byte-identical) — this is a mechanical numbering fix, not a change to any turn's substance, so it doesn't violate transcript append-only/verbatim norms.
- 2026-07-12 (SES-0079 close-out): `set-status <SES> closed` refuses with "session close requires frontmatter participant, participant-role, facilitator" when an already-created session is missing those keys entirely — there is NO sanctioned write op to add/edit arbitrary frontmatter fields on an existing artifact (only `create --field` at creation time). This is a genuine IDEA-0042-class API-unreachable surface (like the body-H1/title gaps already cataloged there); refuse-and-report rather than attempting any workaround — the refusal names no in-API alternative, so report it verbatim and leave the session open. Precedent (SES-0077/SES-0078) avoided this by supplying `--field 'participant: ...'` etc. at `create` time; there is no retroactive fix available to the librarian.
- 2026-07-12: `create` accepts all four session close-required fields (participant/participant-role/facilitator/transcript-fidelity) via repeated `--field 'key: value'` at create time, sidestepping the SES-0079-class no-retroactive-fix trap entirely — do this by default for every new session create, don't wait to be asked. Also reconfirmed: rule-18 relates-to-body-mention (DEC-0250) applies even to a still-`open` session with fresh relates-to links; if the composed body doesn't naturally mention every linked ID, add one, `read section <id> <heading>` for the `--if-match` token first, then `edit-section --from-file` (not `--text`, which `edit-section` does not accept) with the token+new sentence appended to the preserved original text.
- 2026-07-12 (DEC-0417, generalizing SES-0079/DEC-0384's session-only fix): `create` now refuses ANY artifact type listed in the `REQUIRED_AT_CREATE` table (gw_write.py) unless every field the table lists for that type arrives via `--field` — currently one row, sessions, requiring participant/participant-role/facilitator/transcript-fidelity (same four as `SESSION_CLOSE_FIELDS`). The refusal names the type, the missing fields, and `--field` syntax; it runs AFTER per-field value validation, so a bad-value error still takes precedence over a missing-field one. When a `create` is refused for missing fields on any type, check `REQUIRED_AT_CREATE` and the `references/operations.md` create-section entry rather than assuming it's the old session-only trap — future types can be added to the table with no new guard code, only a new row plus (automatically) new test coverage via the existing table-driven guard test.
- 2026-07-12 (SES-0081 close-out, CORRECTS the prior "compose with ### T99" entry): the auto-numbering regex only scans for `###`/`**`-prefixed turn headings, so on an artifact whose EXISTING turns are plain unprefixed text (e.g. "T1 (stakeholder...):" with no `###`), append-turn computes max-seen-turn as 0 regardless of how many real turns exist — a correctly-formatted `### T99` placeholder still gets assigned the wrong number (here: T1, colliding with the file's real existing T1) because the tool never saw the prior turns as headings at all. Check the artifact's actual turn-heading formatting (`grep -n '^### T\|^T[0-9]'`) before trusting `--expect-first-turn`/the assigned number on legacy transcripts. Worse: once the session is `closed`, `edit-section`/`--if-match` refuses outright ("closed sessions are immutable — use append-turn --enrichment ... or open a new session") — there is no mechanical fix-the-digit repair available post-close (the pre-close repair path from the prior entry only works before closing). The full `check` gate does NOT flag the resulting duplicate/mislabeled turn number as an error (rule 21 only checks real `##`/`###` Markdown headings, and unprefixed legacy turn lines aren't headings) — so this can silently pass check and land as a real, permanent transcript defect. If closing is part of the same task, verify the assigned turn number against the transcript's actual last turn BEFORE running set-status closed, so there's still a window to catch and correct it.
- 2026-07-12: opening a fresh full-grilling SES from a caller-supplied verbatim T1-T5 transcript (proposal/addition/restatement/confirmation/alignment shape) is a single `create --type session` call — no batch needed: pass all four SESSION_CLOSE_FIELDS (participant/participant-role/facilitator/transcript-fidelity) plus `kind`/`intake` via --field, the composed multi-turn Transcript section via --from-file (status defaults to 'open' for type=session, correct for an in-progress intake), leave 'Decisions Produced' explicitly stating none-yet since the session is still open. Verified via `read turns <id> T1-T5\' after create -- turn text lands byte-identical to the supplied payload, no renumbering surprises when all turns are supplied in one create (append-turn's numbering quirks only apply to later appends).
- 2026-07-12 (SES-0082 turn-append + overview refresh): `write update-overview --if-match` requires the token from a `read overview` of that SAME artifact — a token from a different `read section` call on the same artifact is a different version-token namespace and gets refused ("overview changed since your read (token mismatch)") even though nothing actually changed; always `read overview <id>` immediately before `update-overview`, never reuse a section/outline token. Also reconfirmed the 250-word overview cap is enforced pre-write (not just advisory) — a first-draft overview covering many new turns came in at 448 words and needed trimming to 217 before the call would even attempt the token check.
- 2026-07-12 (SES-0083): rule 16/DEC-0247 cites-sync (CITES_SYNC_TYPES in check_links.py) covers ONLY business-goal/epic/story/spike/component — decisions are explicitly exempt, so a new decision's body freely mentions other DEC IDs by bare ID without needing add-cite for them, and `add-cite` on an ALREADY-ACCEPTED decision is refused anyway ("accepted decisions are immutable — supersede instead", the check is on the CITING artifact's own status, not the target's) — don't bother trying post-accept; if a decision needs a specific cite, pass it via `--cite` at `create`/`supersede` creation time, not after. `write supersede`'s own `--cite` flag exists for exactly this. For epics/CMPs/spikes (the types that DO need cites-sync), the same rule still applies as documented in earlier entries.
- 2026-07-12 (SES-0083): `write supersede OLD --session S --source-span ... --title ... --overview ... --from-file ...` is the correct single op for "create DEC-A superseding DEC-OLD" — it auto-sets links (derives-from=[session], supersedes=[OLD]), auto-stamps decided-by/decided-on/source-span, flips OLD to superseded+superseded-by, and reports ratified citers of OLD as a stale-walk hint (here: DEC-0348 accepted + EP-0009 approved) — treat that hint list as the review worklist. It does NOT take a `--link relates-to=...` flag, so additional relates-to links (to other decisions the task specifies) are separate `add-link` follow-ups after the ID is known.
- 2026-07-12 (SES-0083): repairing an approved epic's body (DEC-0267 stale->re-affirm in practice) does NOT require an actual status transition through `stale` — `edit-section --amend --if-match <token>` works directly on `approved` status and the artifact simply stays `approved` throughout; "re-affirm rather than re-gate" cashes out as "no status transition at all", just the content edit plus (if the edit adds a new DEC mention) a same-batch `add-cite` since epics ARE cites-sync-checked.
- 2026-07-12 (SES-0082, second visit): a two-turn append-turn call composing both new turns in ONE --from-file payload (### T-headed, real digit placeholders) auto-assigned T22-T23 correctly against a session whose PRIOR turns all use ### T-heading style — reconfirms the numbering quirk from the earlier SES-0081 entry is specific to legacy unprefixed-turn artifacts, not a general risk; no need to append one-turn-at-a-time when the existing transcript already uses ### T headings.
- 2026-07-12 (SES-0082, fourth visit): append-turn's auto-numbering continued clean T28->T29-T30 in a single --from-file two-turn payload (### T99 placeholders x2) against this session's established ### T-heading style, reconfirming the pattern is now routine/reliable for this artifact. No-overview-refresh instruction (elaboration exchange, no new positions) was honored as literally stated by the caller — task explicitly waived the usual read-overview-before-considering-done step.
- 2026-07-12 (SES-0082, fifth visit): a two-part task (append clarification turns THEN park the raised scenario as a captured Idea) is well-served by ONE `write apply` batch in task order — append-turn(session) + create(idea, derives-from session, relates-to a same-topic decision, proposed-by field) + add-link(session relates-to new idea) — with the turn text predicting the not-yet-allocated IDEA-ID (checked `ls docs/ideas | tail -1` immediately beforehand) and mentioning it by bare ID in the facilitator turn; batch's deferred forward-reference resolution (see the DEC-0401 entry above) makes this land clean in one shot with the cross-reference (DEC-0260) satisfied for free, no separate edit-section needed. `create --type idea` needs both `--field 'proposed-by: X'` and frontmatter `links.relates-to` pointed at the decision it clarifies; the session's own relates-to→idea link still needs a separate `add-link` (not settable from the idea's side, and relates-to is never auto-reciprocal per earlier entries).
- 2026-07-12 (SES-0082, DEC-0267 cascade EXECUTED, sixth visit): the actual narrowing-edit batch this repo's DEC-0267 mechanism cashes out to (confirmed for real, not just planned): 6-op `write apply` = 1x append-turn (stakeholder "Reaffirmed" + facilitator note) + 4x edit-section --amend (BG-0001 Intent, BG-0001 System Context, EP-0006 Why, EP-0008 Why — quoted-fragment alignment across artifacts must be done in the SAME batch so a body quote of another artifact's old phrasing doesn't go stale) + 1x update-overview (BG-0001, since Intent's meaning-bearing phrase changed) — zero status transitions (all three stayed `approved` throughout), zero add-cite needed (DEC-0420 was already in all three cites: lists from the DEC-0420 grilling round itself). "Stamp the re-affirmation per the established mechanism" has NO dedicated frontmatter field anywhere in the corpus (grepped for reaffirmed-by/reaffirmed-in/amended-in style keys, found none) — the mechanism IS the content edit itself plus the session transcript turn naming it, exactly as DEC-0267/DEC-0279 describe ("stale marks/edits are the durable record"). `search audit SES-nnnn` (not just `audit <single-DEC>`) works directly on a session artifact — its `considered` set pulls every DEC the session's own relates-to already carries (all 21 new DECs plus every DEC cited in T1-T45), so caller-named already-dispositioned exclusions were already baked in for free with zero manual filtering; remaining candidates were dominated by tooling/checker/Uses-line self-reference noise (the session transcript discusses gw mechanics extensively) — only DEC-0265 (repo-as-coordination-medium, draws the paradigm/app boundary), DEC-0036 (two-layer gate enforcement), DEC-0234 (RoleResolution graduation), DEC-0019 (Groundwork self-specifies) scored as genuinely theme-relevant on manual read; the rest of the top-25 were confidently noise despite high cosine scores — score alone is not a relevance signal here, read every candidate's overview.
- 2026-07-12 (SES-0082, corrective restore): a prior task instructed "only STAMP the re-affirmation" but a librarian instead re-edited the re-affirmed artifacts a second time (new wording never seen by the stakeholder) -- corrected by a plain restore-to-exact-text `write apply` batch (append-turn correction note + edit-section x4 --amend, each payload built by reading the CURRENT on-disk section fresh via `read section` and comparing byte-for-byte against the caller's exact target text before writing, not by re-deriving prose from memory of what it "should" say) + one update-overview. Key gotcha: `write apply` is atomic across the WHOLE batch, not per-op -- when one op's --from-file path was a typo (file didn't exist), the batch printed the earlier op's "OK" line before failing, but the rollback reverted that earlier op too ("rolled back: corpus restored to its pre-op state"); do not trust an op's "OK" line as durable until the whole apply call exits 0. Confirmed again: BG-0001's overview `--if-match` token stayed IDENTICAL across two edit-section calls on other sections (Intent, System Context) in the same batch plus the standalone update-overview call after -- section edits do not bump the overview's own token, only an actual update-overview call does (or another edit to the overview itself); re-reading the overview token before update-overview is still correct practice but proved unnecessary here since it hadn't moved.
- 2026-07-12 (SES-0082, recall-audit disposition landed, seventh visit): a "stamp N relates-to enrichment links onto already-accepted decisions" task is genuinely NOT a refusal risk — operations.md is explicit that accepted decisions accept relates-to enrichment (DEC-0248) as a normal add-link, no --amend/supersede needed; a 19-op add-link-only `write apply` batch (all targets pre-verified accepted, no self-links) landed 100% clean with zero refusals in one shot. Order: append-turn (auto-numbered correctly against this session's ### T-heading style, T48->T49-T50) run standalone first, then the add-link batch, then a fresh `read overview`+`update-overview` (confirmed again: neither append-turn nor add-link bumps the overview's own --if-match token — always re-read immediately before update-overview anyway since it's cheap and the one case that does bump it (another update-overview or a section edit) isn't worth risking).
- 2026-07-12 (SES-0082 final close-out, ninth/final visit): a "close the session" task with pre-scripted verbatim final turns + status transition + overview refresh is best done as three sequential standalone calls, not one apply batch, since `set-status closed` itself has ratifying-transition placeholder/heading guards that should run against the ALREADY-appended final turns (order: append-turn first, then set-status closed, then update-overview last since only a real overview edit bumps its own --if-match token — reconfirmed yet again that append-turn/set-status don't move it, so the token read right after append-turn was still valid at update-overview time). `set-status` has no `closed-on`/date CLI flag at all (checked --help) — the API auto-stamps close date server-side; a task instruction like "closed-on: <date>" is satisfied by trusting the transition's own stamp, not by hunting for a flag. Full end-to-end SES-0082 close pattern across all nine visits this session is now a complete worked example: intake open -> multi-round grilling turns -> batch decision creation -> DEC-0267 re-affirm cascade -> idea capture x3 -> recall-audit disposition (add-link only) -> final verbatim turns -> set-status closed -> update-overview -> full check, all via `write apply`/standalone ops with zero freehand file edits.
